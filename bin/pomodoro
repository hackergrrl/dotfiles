#!/usr/bin/env bash

notify-send "pomodoro STARTED -- you can do it!"
sleep 900
notify-send '10 minutes left' -t 20000
sleep 420
notify-send '3 minutes left' -t 20000
sleep 180
notify-send 'pomodoro COMPLETE -- nice job cutie ^__^' -t 60000
